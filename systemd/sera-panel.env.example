# Örnek environment dosyası (systemd)
#
# Kullanım:
#   sudo cp systemd/sera-panel.env.example /etc/sera-panel.env
#   sudo nano /etc/sera-panel.env
#   sudo chmod 600 /etc/sera-panel.env
#
# Not: Bu dosya repo içinde örnektir; gerçek token/anahtarları repoya koyma.

# 0 = gerçek donanım, 1 = simülasyon
SIMULATION_MODE=0

# Serav1 UI bayrağı (0 = eski UI, 1 = yeni UI)
USE_NEW_UI=0

# Admin endpointleri için token (kendi değerini ver)
ADMIN_TOKEN=change-me

# ESP32 node auth ve limitler (opsiyonel)
# NODE_TOKENS=node1:token1,node2:token2
# NODE_RATE_LIMIT_SECONDS=0.2
# NODE_COMMAND_RATE_LIMIT_SECONDS=0.2
# NODE_COMMAND_TTL_SECONDS=30
# NODE_COMMAND_MAX_QUEUE=50
# NODE_STALE_SECONDS=15

# Opsiyonel (ileride/varsa entegrasyonlar)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
# HA_BASE_URL=http://192.168.1.13:8123
# HA_TOKEN=
